{
  "id": "cloudflare-app-builder",
  "type": "agent",
  "name": "Cloudflare App Builder",
  "description": "An agent that builds full-stack web apps on Cloudflare Workers — React + Vite SPA with a Worker API backend — and ships every project to GitHub. It builds; it does not deploy.",
  "version": "0.3.1",
  "eve": ">=0.1.0",
  "model": "z-ai/glm-5.2",
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.5",
    "zod": "^4.4.3"
  },
  "files": [
    "instructions.md",
    "agent.ts",
    "sandbox.ts",
    "skills/react-on-workers.md"
  ],
  "sandbox": {
    "bootstrap": [
      "apt-get update && apt-get install -y --no-install-recommends ca-certificates curl git jq && rm -rf /var/lib/apt/lists/*",
      "command -v gh >/dev/null || (apt-get update && apt-get install -y --no-install-recommends ca-certificates curl && mkdir -p -m 755 /etc/apt/keyrings && curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg -o /etc/apt/keyrings/githubcli-archive-keyring.gpg && chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg && echo \"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main\" > /etc/apt/sources.list.d/github-cli.list && apt-get update && apt-get install -y --no-install-recommends gh && rm -rf /var/lib/apt/lists/*)",
      "npm install -g create-cloudflare@latest wrangler@latest"
    ],
    "revalidationKey": "cloudflare-app-builder-sandbox@2026-07-06"
  },
  "secrets": [
    {
      "name": "GITHUB_TOKEN",
      "description": "Personal access token with repo access to the organisation — the agent's gh CLI reads it from its shell environment.",
      "sandbox": true
    },
    {
      "name": "GITHUB_ORG",
      "description": "The GitHub organisation the agent creates repos in and works under.",
      "sandbox": true
    }
  ]
}
