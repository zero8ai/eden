# Codex skill metadata for `work-issue`.
#
# This is Codex's equivalent of Claude Code's `disable-model-invocation: true`
# (set in ../SKILL.md frontmatter). It keeps the skill explicit-only:
# Codex will NOT implicitly invoke it from a matching prompt — the user must
# invoke it explicitly with `$work-issue` (or via the `/skills` selector).
interface:
  display_name: "work-issue"
  short_description: "Take a GitHub issue to a review-ready PR: implement, browser-verify, satisfy CI."

policy:
  allow_implicit_invocation: false
