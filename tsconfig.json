{
  "include": [
    "**/*",
    "**/.server/**/*",
    "**/.client/**/*",
    ".react-router/types/**/*"
  ],
  "exclude": [
    // The catalog seed's template files are customer-facing exemplars that import `eve` (not an
    // Eden dependency) — they ship to customer repos, they aren't Eden source. Don't typecheck
    // them here; the catalog CI (catalog/scripts/validate.mjs) gates them instead.
    "catalog/templates/**/*",
    // The bundled assistant eve project (docs/ASSISTANT.md) is a separate project compiled by
    // `eve build` inside its own image — it imports `eve` (not an Eden dependency), so the
    // control-plane tsc must not typecheck it.
    "assistant-template/**/*",
    "mobile/**/*",
    "packages/*/dist/**/*"
  ],
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "types": ["node", "vite/client"],
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "rootDirs": [".", "./.react-router/types"],
    "paths": {
      "~/*": ["./app/*"]
    },
    "esModuleInterop": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "strict": true
  }
}
