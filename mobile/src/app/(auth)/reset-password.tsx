import { router, useLocalSearchParams } from "expo-router";
import { useState } from "react";
import { Text } from "react-native";
import { Button, Card, ErrorState, FormField, Heading, Screen, colors } from "@/components/native";
import { authClient } from "@/lib/auth-client";

export default function ResetPasswordScreen(){const params=useLocalSearchParams<{token?:string}>();const[pw,setPw]=useState("");const[confirm,setConfirm]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);if(!params.token)return <Screen><ErrorState message="This reset link is missing its token or has expired."/></Screen>;const submit=async()=>{if(pw.length<8)return setError("Use at least 8 characters.");if(pw!==confirm)return setError("Passwords do not match.");setBusy(true);setError(null);try{const result=await authClient.resetPassword({newPassword:pw,token:params.token!});if(result.error)return setError(result.error.message??"Could not reset your password.");router.replace("/(auth)/login")}catch(cause){setError(cause instanceof Error?cause.message:"Could not reset your password.")}finally{setBusy(false)}};return <Screen><Heading title="Choose a new password" subtitle="Your password will be updated on all devices."/><Card><FormField label="New password" value={pw} onChangeText={setPw} secureTextEntry autoComplete="new-password"/><FormField label="Confirm password" value={confirm} onChangeText={setConfirm} secureTextEntry onSubmitEditing={submit}/>{error?<Text accessibilityRole="alert" style={{color:colors.red}}>{error}</Text>:null}<Button title={busy?"Updating…":"Update password"} disabled={busy} onPress={submit}/></Card></Screen>}
