{
  "id": "reviewer",
  "type": "agent",
  "name": "Reviewer",
  "description": "Reviews open pull requests — reads the change against its issue, runs the repo's checks, and leaves specific, ranked feedback. Reviews; doesn't merge.",
  "version": "0.1.0",
  "eve": ">=0.1.0",
  "model": "z-ai/glm-5.2",
  "includes": [{ "type": "skill", "id": "reviewing-a-pull-request" }],
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.5",
    "zod": "^4.4.3"
  },
  "files": ["instructions.md", "agent.ts", "sandbox.ts"],
  "sandbox": {
    "bootstrap": [
      "apt-get update && apt-get install -y --no-install-recommends ca-certificates curl git jq unzip zip rsync openssh-client build-essential pkg-config python3 python3-pip ripgrep fd-find shellcheck less nano && rm -rf /var/lib/apt/lists/*",
      "command -v fd >/dev/null || (command -v fdfind >/dev/null && ln -s \"$(command -v fdfind)\" /usr/local/bin/fd) || true",
      "command -v gh >/dev/null || (apt-get update && apt-get install -y --no-install-recommends ca-certificates curl && mkdir -p -m 755 /etc/apt/keyrings && curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg -o /etc/apt/keyrings/githubcli-archive-keyring.gpg && chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg && echo \"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main\" > /etc/apt/sources.list.d/github-cli.list && apt-get update && apt-get install -y --no-install-recommends gh && rm -rf /var/lib/apt/lists/*)",
      "corepack enable || true",
      "set -e; command -v pnpm >/dev/null 2>&1 || npm install -g pnpm@latest; command -v yarn >/dev/null 2>&1 || npm install -g yarn@latest"
    ],
    "revalidationKey": "reviewer-sandbox@2026-07-07"
  },
  "secrets": [
    {
      "name": "GITHUB_TOKEN",
      "description": "GitHub token with read access to the repositories this agent reviews and permission to comment on and submit reviews for their pull requests. The agent discovers accessible repositories from this token when the target is not explicit.",
      "sandbox": true
    }
  ]
}
