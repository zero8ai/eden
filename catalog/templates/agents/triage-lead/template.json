{
  "id": "triage-lead",
  "type": "agent",
  "name": "Triage Lead",
  "description": "Keeps a GitHub issue backlog healthy — triages, labels, prioritizes, de-duplicates, and assigns work through gh, learning each repo's own taxonomy. Runs on demand or on a schedule.",
  "version": "0.2.0",
  "eve": ">=0.1.0",
  "model": "z-ai/glm-5.2",
  "includes": [
    { "type": "skill", "id": "triaging-issues" }
  ],
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.5",
    "zod": "^4.4.3"
  },
  "files": ["instructions.md", "agent.ts", "sandbox.ts"],
  "sandbox": {
    "bootstrap": [
      "apt-get update && apt-get install -y --no-install-recommends ca-certificates curl git jq && rm -rf /var/lib/apt/lists/*"
    ],
    "revalidationKey": "triage-lead-sandbox@2026-07-09"
  }
}
