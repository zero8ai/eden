import { discordChannel } from "eve/channels/discord";

// Credentials come from the DISCORD_APPLICATION_ID / DISCORD_BOT_TOKEN /
// DISCORD_PUBLIC_KEY environment variables (set them as agent secrets in Eden).
export default discordChannel({});
