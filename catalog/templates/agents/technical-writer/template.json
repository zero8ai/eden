{
  "id": "technical-writer",
  "type": "agent",
  "name": "Technical Writer",
  "description": "Writes and maintains a project's documentation — READMEs, guides, references, changelogs — as pull requests, accurate to the code and in the project's own voice.",
  "version": "0.1.0",
  "eve": ">=0.1.0",
  "model": "z-ai/glm-5.2",
  "includes": [{ "type": "skill", "id": "technical-writing" }],
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.5",
    "zod": "^4.4.3"
  },
  "files": ["instructions.md", "agent.ts", "sandbox.ts"],
  "sandbox": {
    "bootstrap": [
      "apt-get update && apt-get install -y --no-install-recommends ca-certificates curl git jq unzip zip rsync openssh-client ripgrep fd-find less nano && rm -rf /var/lib/apt/lists/*",
      "command -v fd >/dev/null || (command -v fdfind >/dev/null && ln -s \"$(command -v fdfind)\" /usr/local/bin/fd) || true",
      "command -v gh >/dev/null || (apt-get update && apt-get install -y --no-install-recommends ca-certificates curl && mkdir -p -m 755 /etc/apt/keyrings && curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg -o /etc/apt/keyrings/githubcli-archive-keyring.gpg && chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg && echo \"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main\" > /etc/apt/sources.list.d/github-cli.list && apt-get update && apt-get install -y --no-install-recommends gh && rm -rf /var/lib/apt/lists/*)",
      "corepack enable || true"
    ],
    "revalidationKey": "technical-writer-sandbox@2026-07-07"
  },
  "secrets": [
    {
      "name": "GITHUB_TOKEN",
      "description": "GitHub token with read/write access to the repositories this agent documents and permission to open pull requests. The agent discovers accessible repositories from this token when the target is not explicit.",
      "sandbox": true
    }
  ]
}
