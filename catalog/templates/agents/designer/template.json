{
  "id": "designer",
  "type": "agent",
  "name": "Designer",
  "description": "A product designer that turns problems into build-ready design specs and critiques UI work — capturing decisions and feedback in GitHub as committed notes and PR/issue comments.",
  "version": "0.3.0",
  "eve": ">=0.22.0",
  "model": "z-ai/glm-5.2",
  "includes": [
    { "type": "skill", "id": "design-critique" }
  ],
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.5",
    "eve": "^0.22.0",
    "zod": "^4.4.3"
  },
  "files": ["instructions.md", "agent.ts", "sandbox.ts"],
  "sandbox": {
    "bootstrap": [
      "apt-get update && apt-get install -y --no-install-recommends ca-certificates curl git jq && rm -rf /var/lib/apt/lists/*"
    ],
    "revalidationKey": "designer-sandbox@2026-07-09"
  }
}
