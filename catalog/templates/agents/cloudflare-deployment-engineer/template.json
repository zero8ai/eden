{
  "id": "cloudflare-deployment-engineer",
  "type": "agent",
  "name": "Cloudflare CI Engineer",
  "description": "An agent that sets up and operates CI/CD for Cloudflare apps from GitHub — gh + wrangler/API with sandbox-exposed credentials, no bespoke tools.",
  "version": "0.2.2",
  "eve": ">=0.1.0",
  "model": "z-ai/glm-5.2",
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.5",
    "zod": "^4.4.3"
  },
  "files": [
    "instructions.md",
    "agent.ts",
    "sandbox.ts"
  ],
  "secrets": [
    {
      "name": "GITHUB_TOKEN",
      "description": "GitHub token with repo write, workflow, pull request, and Actions secrets access for the organisation's repos — the agent's gh CLI reads it from its shell environment.",
      "sandbox": true
    },
    {
      "name": "GITHUB_ORG",
      "description": "The GitHub organisation the deployable repos live in.",
      "sandbox": true
    },
    {
      "name": "CLOUDFLARE_API_TOKEN",
      "description": "API token for the target account with Workers edit permissions plus any product permissions the app needs, such as KV, D1, R2, Queues, Pages, Routes, or DNS.",
      "sandbox": true
    },
    {
      "name": "CLOUDFLARE_ACCOUNT_ID",
      "description": "The Cloudflare account id the apps deploy into.",
      "sandbox": true
    }
  ]
}
